"use client";
import React from 'react';
import styled from 'styled-components';

export type AssistChipProps = {
	// types...
}

const AssistChip: React.FC<AssistChipProps>  = ({}) => {
	return (
		<AssistChipStl>
 			AssistChip works!
 		</AssistChipStl>
	);
};

export const AssistChipStl = styled.div``;

export default AssistChip;
