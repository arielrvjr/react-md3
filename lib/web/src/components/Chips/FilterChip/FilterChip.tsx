"use client";
import React from 'react';
import styled from 'styled-components';

export type FilterChipProps = {
	// types...
}

const FilterChip: React.FC<FilterChipProps>  = ({}) => {
	return (
		<FilterChipStl>
 			FilterChip works!
 		</FilterChipStl>
	);
};

export const FilterChipStl = styled.div``;

export default FilterChip;
