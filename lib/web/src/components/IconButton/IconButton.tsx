"use client";
import React from 'react';
import styled from 'styled-components';

export type IconButtonProps = {
	// types...
}

const IconButton: React.FC<IconButtonProps>  = ({}) => {
	return (
		<IconButtonStl>
 			IconButton works!
 		</IconButtonStl>
	);
};

export const IconButtonStl = styled.div``;

export default IconButton;
